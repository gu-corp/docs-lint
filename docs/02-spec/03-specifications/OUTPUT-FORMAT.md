# 出力フォーマット仕様

**バージョン**: 2.0.0
**更新日**: 2026-02-18

---

## 概要

docs-lint の各出力フォーマット（テキスト、JSON、AIプロンプト）の構造を定義します。

## 出力モード

| モード | オプション | 用途 |
|--------|-----------|------|
| テキスト | (デフォルト) | 人間が読みやすい形式 |
| JSON | `--json` | プログラム処理用 |
| AIプロンプト | `--ai-prompt` | AI評価用プロンプト生成 |

---

## テキスト出力（デフォルト）

### 基本構造

```text
📄 Documentation Lint Results

Files checked: {ファイル数}
Status: {PASSED|FAILED}

Summary:
  Errors: {エラー数}
  Warnings: {警告数}
  Passed: {合格ルール数}

{ルール結果一覧}

💡 Tip: {ヒントメッセージ}
```

### ルール結果の表示

```text
# 合格ルール
✓ brokenLinks

# 警告ルール
⚠ todoComments (2 issues)
    README.md:45 [TODO] 実装予定: サンプルを追加
    GUIDE.md:12 [FIXME] 要修正: エラーハンドリング

# エラールール
✗ standardFolderStructure (1 issues)
    Missing required folder: 01-plan/
```

### ステータス記号

| 記号 | 意味 |
|------|------|
| `✓` | 合格（問題なし） |
| `⚠` | 警告（改善推奨） |
| `✗` | エラー（修正必須） |

### 詳細出力（`-v, --verbose`）

詳細モードでは、すべての問題を省略せずに表示します。

```text
⚠ todoComments (10 issues)
    README.md:45 [TODO] 実装予定: サンプルを追加
    README.md:67 [TODO] 実装予定: テストを追加
    GUIDE.md:12 [FIXME] 要修正: エラーハンドリング
    ... （全件表示）
```

---

## JSON出力（`--json`）

### 基本構造

```json
{
  "meta": {
    "generatedAt": "2026-02-18T00:00:00.000Z",
    "docsDir": "./docs",
    "tool": "@gu-corp/docs-lint"
  },
  "summary": {
    "totalFiles": 15,
    "errors": 0,
    "warnings": 3,
    "passed": true
  },
  "rules": [...],
  "issues": [...]
}
```

### meta オブジェクト

| フィールド | 型 | 説明 |
|-----------|-----|------|
| `generatedAt` | `string` | ISO 8601形式の生成日時 |
| `docsDir` | `string` | ドキュメントディレクトリパス |
| `tool` | `string` | ツール名（固定値） |

### summary オブジェクト

| フィールド | 型 | 説明 |
|-----------|-----|------|
| `totalFiles` | `number` | チェック対象ファイル数 |
| `errors` | `number` | エラー数 |
| `warnings` | `number` | 警告数 |
| `passed` | `boolean` | 全体の合否 |

### rules 配列

```json
{
  "rules": [
    {
      "name": "brokenLinks",
      "severity": "error",
      "issueCount": 0,
      "passed": true
    },
    {
      "name": "todoComments",
      "severity": "warn",
      "issueCount": 2,
      "passed": true
    }
  ]
}
```

| フィールド | 型 | 説明 |
|-----------|-----|------|
| `name` | `string` | ルール名 |
| `severity` | `string` | 重要度（`error`/`warn`/`off`） |
| `issueCount` | `number` | 検出問題数 |
| `passed` | `boolean` | このルールの合否 |

### issues 配列

```json
{
  "issues": [
    {
      "rule": "todoComments",
      "severity": "warn",
      "file": "README.md",
      "line": 45,
      "message": "[TODO] 実装予定: サンプルを追加",
      "suggestion": null
    },
    {
      "rule": "brokenLinks",
      "severity": "error",
      "file": "GUIDE.md",
      "line": null,
      "message": "Broken link to \"./MISSING.md\"",
      "suggestion": "Verify the file exists or update the link"
    }
  ]
}
```

| フィールド | 型 | 説明 |
|-----------|-----|------|
| `rule` | `string` | ルール名 |
| `severity` | `string` | 重要度 |
| `file` | `string` | ファイルパス（相対） |
| `line` | `number?` | 行番号（1始まり、null可） |
| `message` | `string` | 問題の説明 |
| `suggestion` | `string?` | 修正提案（null可） |

### 完全な例

```json
{
  "meta": {
    "generatedAt": "2026-02-18T10:30:00.000Z",
    "docsDir": "./docs",
    "tool": "@gu-corp/docs-lint"
  },
  "summary": {
    "totalFiles": 28,
    "errors": 1,
    "warnings": 5,
    "passed": false
  },
  "rules": [
    {
      "name": "brokenLinks",
      "severity": "error",
      "issueCount": 1,
      "passed": false
    },
    {
      "name": "todoComments",
      "severity": "warn",
      "issueCount": 3,
      "passed": true
    },
    {
      "name": "headingHierarchy",
      "severity": "warn",
      "issueCount": 0,
      "passed": true
    }
  ],
  "issues": [
    {
      "rule": "brokenLinks",
      "severity": "error",
      "file": "03-guide/GETTING-STARTED.md",
      "line": null,
      "message": "Broken link to \"./SETUP.md\"",
      "suggestion": "Verify the file exists or update the link"
    },
    {
      "rule": "todoComments",
      "severity": "info",
      "file": "README.md",
      "line": 45,
      "message": "[TODO] 実装予定: サンプルを追加",
      "suggestion": null
    }
  ]
}
```

---

## AIプロンプト出力（`--ai-prompt`）

### 構造概要

```markdown
# ドキュメント品質評価レポート

<ai-instructions>
## AIへの指示
{ペルソナ、評価手順、重要事項}
</ai-instructions>

---

## 0. ドキュメント標準規約
{DOCUMENT_STANDARDS.md の内容}

---

## 1. 概要
{ファイル数、エラー/警告数}

## 2. フォルダ構成
{ツリー表示}

## 3. 検出された問題
### 3.1 エラー（修正必須）
### 3.2 警告（改善推奨）

## 4. 品質メトリクス
{評価チェックリスト}

## 5. 評価依頼
{AIへの具体的な評価依頼}
```

### セクション詳細

#### AIへの指示（ai-instructions）

AIに以下のペルソナを指定：

- アーキテクト: システム全体の整合性、設計の一貫性
- エンジニア: 実装可能性、技術的正確性
- テスト設計者: テスト可能性、要件の追跡可能性
- セキュリティエンジニア: セキュリティ考慮事項の有無

#### フォルダ構成（ツリー形式）

```text
docs/
├── 01-plan/
│   ├── PROPOSAL.md
│   └── ROADMAP.md
├── 02-spec/
│   ├── 01-requirements/
│   │   └── REQUIREMENTS.md
│   └── 02-architecture/
│       └── ARCHITECTURE.md
└── README.md
```

#### 品質メトリクス

```markdown
### 4.1 構造的完全性
- [ ] 必須ドキュメントが揃っているか
- [ ] フォルダ構成が論理的か
- [ ] 命名規則が統一されているか

### 4.2 内容の一貫性
- [ ] 用語が統一されているか
- [ ] 相互参照が適切か

### 4.3 保守性
- [ ] バージョン情報が明記されているか
- [ ] 関連ドキュメントへのリンクがあるか

### 4.4 完成度
- [ ] TODO/FIXMEが残っていないか

### 4.5 要件追跡
- [ ] FR-XXX形式の要件IDが定義されているか
- [ ] TC-XXX形式のテストケースIDが定義されているか
```

#### 評価依頼

AIに以下を依頼：

1. 総合スコア (1-10)
2. 優先度の高い改善点（最大5件）
3. 良い点（最大3件）
4. 次のアクション（具体的な改善提案）

---

## 終了コード

| コード | 意味 |
|--------|------|
| `0` | 全チェック合格（エラーなし） |
| `1` | 1つ以上のエラー検出 |

**注意**: 警告のみの場合は終了コード `0` を返します。

---

## 出力先

| 出力 | 出力先 |
|------|--------|
| 通常出力 | stdout |
| エラーメッセージ | stderr |
| JSON出力 | stdout |
| AIプロンプト | stdout |

### リダイレクト例

```bash
# JSON出力をファイルに保存
docs-lint lint --json > lint-report.json

# AIプロンプトをファイルに保存
docs-lint lint --ai-prompt > quality-report.md

# エラーのみ表示
docs-lint lint 2>&1 | grep -E "^(✗|Error)"
```

---

## 関連ドキュメント

- [CLIリファレンス](../../03-guide/CLI.md)
- [設定スキーマ仕様](./CONFIG-SCHEMA.md)
- [ルール実装詳細](./RULES-IMPLEMENTATION.md)
